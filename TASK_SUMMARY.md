# 代码结构分析任务清单

## 任务状态

- [x] **#1 Explore project context** - 检查项目文件结构、配置和文档
- [x] **#2 分析 crates 结构** - 理解 gateway-core, providers, gateway-server 三个 crate 的职责
- [x] **#3 梳理请求处理流程** - 跟踪请求从入口到响应的完整调用链
- [x] **#4 总结架构设计文档** - 生成详细的架构分析文档

## 产出物

📄 **架构分析文档**：`docs/architecture-analysis.md`

包含内容：
1. 项目概述与整体架构图
2. Crate 结构详解（3 个 crate，每个的核心职责）
3. 请求处理流程（非流式 vs 流式）
4. 核心组件深度解析（认证、限流、配额、熔断器、重试、计量）
5. 关键设计决策分析
6. 性能优化点
7. 安全设计与缺口
8. 企业级特性支持状态

## 核心发现

### 架构亮点

✅ **分层清晰**
- `gateway-core`：核心抽象（配置/类型/错误）
- `providers`：供应商适配器（统一 `LLMProvider` trait）
- `gateway-server`：HTTP 服务（路由/中间件/状态）

✅ **安全基线强**
- API Key HMAC-SHA256 + salt 存储
- 常时比较防时序攻击
- RBAC 两级角色（admin/developer）
- 审计日志可选（JSONL）

✅ **企业特性全**
- 多租户隔离（tenant_id 隔离计量/配额/成本）
- 每租户令牌桶限流（µ-令牌原子实现）
- 熔断器（每供应商独立状态机）
- 重试 + 跨供应商降级

✅ **性能优化到位**
- 热路径原子变量（避免锁）
- `RwLock` 读多写少场景
- 懒初始化（首次请求创建租户桶）
- 全抖动重试（防同步风暴）

### 待改进点

⚠️ **持久化不全**
- 计量事件已落盘（#6 完成）
- **Key Store / 配额仍内存**，重启丢失

⚠️ **计量事件未签名**
- 有文件写权限者可伪造/篡改账单
- 后续需补逐事件 HMAC 签名

⚠️ **无 OTel 链路追踪**
- 仅有 Prometheus + JSON 日志 + 审计
- 缺少分布式追踪上下文传播

⚠️ **路由简单**
- `build_fallback_chain` 硬编码内置供应商优先级
- 缺少 Provider 优先级/权重配置

## 请求流程图（简化版）

```
客户端 → x_request_id → auth (HMAC) → rate_limit (令牌桶) → quota → 路由
                                    ↓
                    非流式：缓存 → retry → provider → 计量 → 返回
                                    ↓
                    流式：估算 → retry → provider → SSE 流
```

## 下一步建议

1. **优先级高**：补计量事件签名（防篡改）
2. **优先级中**：Key Store / 配额持久化（迁移到 DB）
3. **优先级低**：OTel 链路追踪集成
