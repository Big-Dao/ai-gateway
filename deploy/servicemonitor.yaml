apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-gateway
  namespace: ai-gateway
spec:
  selector:
    matchLabels:
      app: ai-gateway
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
